import LobbyPage from "pages/LobbyPage";

const lobby = [
  {
    path: "lobby",
    children: [
      {
        index: true,
        element: <LobbyPage />,
      },
    ],
  },
];

export default lobby;
